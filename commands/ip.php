<?php

function getIP($add) {
  $command = " Please try again.\nIP <address>";
  $add = trim(preg_replace('/\s\s+/', ' ', str_replace("\n", " ", $pokemon)));

  $output = 'adi ka ip';
  $answer = ['text' => $output];
  return $answer;
}